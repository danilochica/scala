object BuclesWhileWoksheet {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(43); 
  
  var k = 0;System.out.println("""k  : Int = """ + $show(k ));$skip(75); 
  
  do{
  	
  	println(s"k vale $k")
  	k = k+  1
  
  }
  while( k < 20)}
  
  
  
  
}
